[project]
name = "rabbitmq-webcam-streaming"
version = "0.1.0"
description = "RabbitMQ client library for Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.13.0.92",
    "pika>=1.3.2",
]
